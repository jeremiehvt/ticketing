$(function() {
/*
	var disableddates = ["05-01", "11-01", "12-25"];
 
	function DisableDates (date){

	    var string = jQuery.datepicker.formatDate('mm-dd', date);
	    var day = date.getDay();


		if (day == 2 && $.inArray(string, disableddates) == -1) {
			return [false];
		} else 
			if (day == 2){
			return [false];
		} else 
			if ($.inArray(string, disableddates) != -1){
				return [false];
			} else {
			return [true];
		}
	}
 
	$( "#command_visitDay" ).datepicker({
	  minDate: new Date(),
	  //beforeShowDay: DisableDates
	});

	$( "#command_visitDay" ).attr('placeholder', 'mois/jour/année');
*/
});
